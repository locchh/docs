# Glue author record, 2026-09-19

Status: checked draft, awaiting independent review. This is an author checklist,
not a reviewer PASS. No commit or AWS account operation was performed.

File: `services/09-analytics/glue.md`, tier M. Body: 4,417 words. Seven topic
sections, Professional depth, worked scenario, exam lens, eight-question quiz,
summary, related units and sources. Twenty-five official pages fetched.

## Coverage

| Planned topic | Teaching section |
|---|---|
| Catalog, crawlers, discovery versus transformation | Catalog data before choosing a processing engine |
| Spark, Python shell, Studio, DataBrew; Ray status | Choose Spark, Python shell or a visual preparation tool |
| CSV conversion, Parquet, partitioning, pushdown | Convert CSV to Parquet and design the partition layout |
| Bookmarks, workflows, triggers, concurrency | Track incremental work and orchestrate dependencies |
| Streaming ETL and Schema Registry | Separate streaming progress from schema contracts |
| IAM, network path, encryption, Data Quality | Secure the data path and make quality a publication gate |
| Pricing, capacity, Auto Scaling, Flex | Match capacity and cost to the useful work |

Owned SAA-C03 3.5-K4 (transformation services and use cases) is taught in the
engine-choice and format-conversion sections. Owned 3.5-S7 (CSV to Parquet) is
taught in the conversion section, illustrated by the code fragment and worked
scenario, and tested in Q1. No SAP guide bullets are assigned to this unit as
sole owner. Its Professional material supports architecture, performance and
modernization decisions without claiming additional ownership.

## High-risk facts checked

- The Ray availability-change page closes new customers from April 30, 2026,
  allows existing use, and recommends Ray on EKS. Its title is not evidence of
  a termination date for existing workloads.
- Crawlers and the catalog discover/store metadata; they do not convert CSV.
- Python shell and streaming jobs do not support job bookmarks.
- Streaming uses checkpoints; batch bookmarks track supported input progress.
- Bookmark reset/rewind does not delete target files.
- Job initialization, commit and transformation contexts affect bookmark state.
- Partition pushdown and catalog-index predicates have different behavior and syntax.
- Conditional trigger chains require the appropriate trigger-started lineage.
- Workflow concurrency rejection is not automatically retried by Glue.
- Job-run queuing is separate from workflow concurrency handling.
- Glue VPC interfaces have private addresses, not public addresses.
- Data Quality evaluation and publication enforcement are separate decisions.
- Lake Formation behavior depends on the runtime and supported access path.
- Flex applies only to eligible workloads; time-sensitive reports need an
  appropriate execution choice.

The Sources section links the documentation used for these facts. No exact
regional prices or broad runtime-support claims were inferred from memory.

## Author checks

Mechanical checker passes; source URL checks pass. Three related links target
planned, unwritten lessons: Athena, Lake Formation and EMR. The Python fragment
parses but is explicitly illustrative, not a complete deployed Glue job.

Quiz: 5 Associate / 3 Professional, 3 multiple-response questions. Author keys:
1 B; 2 C; 3 A/E; 4 A; 5 D; 6 B/D; 7 C; 8 A/E. Every explanation is within the
60-160-word range and dismisses all other options. Every coverage line names an
actual section. Independent blind solving has not occurred.

Remaining gate: a separate reviewer must apply REVIEW_SPEC.md, verify factual
claims, solve all questions and review coverage before this unit is marked done.
